name: Marketplace publish

on:
  workflow_dispatch:
    inputs:
      expected_version:
        description: Version expected in vscode-extension/package.json
        required: true
        type: string
      release_kind:
        description: Marketplace channel to publish. Pre-release versions must not match a regular release version.
        required: true
        default: stable
        type: choice
        options:
          - stable
          - prerelease

permissions:
  contents: read

concurrency:
  group: marketplace-publish
  cancel-in-progress: false

jobs:
  publish:
    name: Publish repository version to Marketplace
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v5

      - name: Read extension version
        id: version
        shell: bash
        run: |
          set -euo pipefail

          VERSION="$(node -e "const pkg = require('./vscode-extension/package.json'); process.stdout.write(pkg.version);")"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

      - name: Verify requested version
        shell: bash
        run: |
          set -euo pipefail

          if [ "${{ inputs.expected_version }}" != "${{ steps.version.outputs.version }}" ]; then
            echo "Requested Marketplace version ${{ inputs.expected_version }} does not match vscode-extension/package.json version ${{ steps.version.outputs.version }}."
            exit 1
          fi

      - name: Validate extension
        shell: bash
        run: |
          set -euo pipefail

          cd vscode-extension
          npm run validate

      - name: Package VSIX from repository version
        shell: bash
        run: |
          set -euo pipefail

          mkdir -p dist
          cd vscode-extension
          package_args=(--out "../dist/bc-dev-toolset-${{ inputs.release_kind }}-${{ steps.version.outputs.version }}.vsix")
          if [ "${{ inputs.release_kind }}" = "prerelease" ]; then
            package_args=(--pre-release "${package_args[@]}")
          fi

          npm run package -- "${package_args[@]}"

      - name: Publish VSIX to VS Code Marketplace
        env:
          VSCE_PAT: ${{ secrets.VSCE_PAT }}
        shell: bash
        run: |
          set -euo pipefail

          prerelease_flag=""
          if [ "${{ inputs.release_kind }}" = "prerelease" ]; then
            prerelease_flag="--pre-release"
          fi

          bash ./.github/scripts/publish-vsix-with-retry.sh \
            "dist/bc-dev-toolset-${{ inputs.release_kind }}-${{ steps.version.outputs.version }}.vsix" \
            "$prerelease_flag"
